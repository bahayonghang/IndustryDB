# 什么是 IndustryDB？

IndustryDB 是一个高性能的数据库中间件，结合了 Rust 的速度和 Python 的简洁性。它为多种数据库提供统一的接口，同时利用 Polars 进行高效的数据操作。

## 核心理念

IndustryDB 建立在三个核心原则之上：

### 🚀 性能优先

采用 Rust 编写并编译为本地代码，IndustryDB 提供卓越的性能：

- 通过 Apache Arrow 格式实现**零拷贝数据传输**
- 通过 ConnectorX 实现**并行查询执行**
- 支持**惰性求值**以优化查询
- PyO3 绑定带来的**最小 Python 开销**

### 🎯 开发者体验

清晰的 Pythonic API，自然易懂：

```python
import industrydb as idb

# 简单直观
conn = idb.Connection.from_uri("postgresql://localhost/mydb")
df = conn.execute("SELECT * FROM users")
conn.close()
```

### 🔌 统一接口

一个 API 适配多种数据库：

- PostgreSQL
- SQLite  
- Microsoft SQL Server

切换数据库无需改变代码结构。

## 核心特性

### 多数据库支持

IndustryDB 原生支持三大主流数据库系统：

| 数据库 | 状态 | 驱动 |
|--------|------|------|
| PostgreSQL | ✅ 完全支持 | ConnectorX |
| SQLite | ✅ 完全支持 | ConnectorX |
| MSSQL | ✅ 完全支持 | ConnectorX |

### Polars 集成

对 Polars DataFrame 的一流支持：

```python
import polars as pl
import industrydb as idb

# 创建 DataFrame
df = pl.DataFrame({
    "id": [1, 2, 3],
    "name": ["Alice", "Bob", "Charlie"]
})

# 直接 DataFrame 操作
conn = idb.Connection.from_uri("sqlite://./data.db")
conn.insert("users", df)
result = conn.select("users")
```

### 类型安全

全面的类型提示，提供出色的 IDE 支持：

- 完整的 `.pyi` 类型桩文件
- 运行时类型检查
- MyPy 兼容
- 现代 IDE 中的自动补全

### CRUD 操作

简洁的 CRUD 操作，支持可选参数：

```python
# 插入
conn.insert("users", {"name": ["Alice"], "age": [25]})

# 带条件查询
df = conn.select("users", where="age > ?", params=[18])

# 更新
conn.update("users", {"age": 26}, where="name = ?", params=["Alice"])

# 删除
conn.delete("users", where="age < ?", params=[18])
```

## 架构概览

IndustryDB 采用模块化的多 crate 架构：

```
┌─────────────────────────────────┐
│     Python 层（用户 API）       │
│  - 简洁的 Python 接口           │
└────────────┬────────────────────┘
             │ PyO3 绑定
┌────────────▼────────────────────┐
│   industrydb-py (Rust→Python)   │
│  - 工厂模式                     │
│  - 异常映射                     │
└─┬──────────┬──────────┬─────────┘
  │          │          │
  ▼          ▼          ▼
┌────────┬────────┬────────┐
│Postgres│ SQLite │ MSSQL  │ 连接器
└───┬────┴───┬────┴───┬────┘
    └────────┼────────┘
             │
    ┌────────▼────────┐
    │ industrydb-core │ 核心 Trait
    │ - Traits        │
    │ - Config        │
    │ - Errors        │
    └─────────────────┘
```

### 设计原则

- **基于 Trait 的抽象**：所有连接器实现通用 trait
- **工厂模式**：运行时选择数据库连接器
- **零拷贝传输**：Arrow 格式实现高效数据交换
- **关注点分离**：层之间有清晰的边界

## 性能

IndustryDB 为性能而生：

- **原生 Rust 代码**：编译为机器码，无解释器开销
- **ConnectorX**：经过实战检验的驱动，支持并行查询
- **Apache Arrow**：列式存储格式，支持零拷贝操作
- **Polars**：快速的 DataFrame 操作，SIMD 优化

基准测试结果显示，对于数据密集型工作负载，IndustryDB 通常比传统 Python 数据库库快 2-5 倍。

## 使用场景

IndustryDB 非常适合：

### 数据工程

- **ETL 管道**：快速的数据提取和转换
- **数据仓库**：批量加载和查询
- **数据迁移**：跨数据库数据传输

### 数据分析

- **探索性分析**：快速查询，DataFrame 输出
- **报告生成**：从多个数据源生成报告
- **数据科学**：与 Polars 生态系统集成

### 应用开发

- **后端服务**：API 的快速数据库访问
- **批处理**：高效的批量操作
- **实时处理**：低延迟查询执行

## 对比

### vs. SQLAlchemy

| 特性 | IndustryDB | SQLAlchemy |
|------|-----------|------------|
| 语言 | Rust + Python | 纯 Python |
| 性能 | ~3-5倍更快 | 基准线 |
| DataFrame | 原生 Polars | Pandas 适配器 |
| API 风格 | 函数式 | ORM + Core |
| 类型提示 | 全面 | 部分 |

### vs. pandas + SQLAlchemy

| 特性 | IndustryDB | pandas + SQLAlchemy |
|------|-----------|---------------------|
| 速度 | 🚀 非常快 | 🐌 较慢 |
| 内存 | ✅ 高效 | ❌ 开销更高 |
| 类型安全 | ✅ 完全 | ⚠️ 有限 |
| 设置 | 简单 | 复杂 |

## 下一步

- [快速开始](/zh/guide/getting-started) - 安装并运行第一个查询
- [配置](/zh/guide/configuration) - 了解配置选项
- [CRUD 操作](/zh/guide/crud-operations) - 掌握数据操作
- [API 参考](/zh/api/connection) - 详细的 API 文档
